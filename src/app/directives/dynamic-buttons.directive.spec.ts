import { DynamicButtonsDirective } from './dynamic-buttons.directive';

describe('DynamicButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
